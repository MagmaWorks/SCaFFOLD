<Window x:Class="Calcs.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Calcs"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <!--<ContentControl x:Key="view2D">
            <local:CalcInputs DataContext="{Binding TBC}"/>
        </ContentControl>-->
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <TabControl>
            <TabItem Header="Single Calculation">
                <local:SingleCalc />
            </TabItem>
            <TabItem Header="Table">
                <local:CrossRefTable DataContext="{Binding Table}"/>
            </TabItem>
            <TabItem Header="Chart">
                <local:ChartControl DataContext="{Binding Chart}"/>
            </TabItem>
            <TabItem Header="Cross Ref">
                <local:CrossRef DataContext="{Binding CrossRef}"/>
            </TabItem>
        </TabControl>
        <Image Grid.Row="1" HorizontalAlignment="Left">
            <Image.Source>
                <BitmapImage UriSource="/WhitbyWood.jpg"/>
            </Image.Source>
        </Image>

    </Grid>

</Window>
