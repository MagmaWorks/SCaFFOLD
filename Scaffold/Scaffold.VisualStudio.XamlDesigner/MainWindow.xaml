<!--
    The XAML designer is not properly supported with VS Extensibility and Hot Reload does not work
    with the XAML designer. Therefore, we have to use the XAML designer in a separate project.
-->
<Window
    Height="600"
    Title="MainWindow"
    Width="410"
    mc:Ignorable="d"
    x:Class="Scaffold.VisualStudio.XamlDesigner.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:maths="clr-namespace:WpfMath.Controls;assembly=WpfMath"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:xaml="clr-namespace:Scaffold.VisualStudio.Models.Xaml;assembly=Scaffold.VisualStudio.Models"
    xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0">
    <Window.DataContext>
        <xaml:MainWindowViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0" Margin="10">
            <Button Name="SimulateOpenTab" Click="OpenTab_Click">Simulate tab opened</Button>

            <Button Name="SimulateFileSaved" Click="FileSaved_Click" Margin="0,10,0,0" Visibility="Collapsed">Simulate file saved</Button>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <Grid Margin="10" d:DataContext="{d:DesignInstance xaml:MainWindowViewModel}">


                <StackPanel Orientation="Vertical" Visibility="{Binding OnLoadVisibility}">
                    <StackPanel Orientation="Horizontal">
                        <!--<imaging:CrispImage Width="24" Height="24" Moniker="{x:Static catalog:KnownMonikers.FileSystemWatcher}" />-->

                        <Label FontSize="14" Margin="10,0,0,0">Watch for changes</Label>
                    </StackPanel>

                    <DockPanel Margin="0,15,0,0">
                        <TextBlock TextWrapping="WrapWithOverflow">
                Starting the watcher will run all calculations
                in the watched project until it is stopped.
                It will provide instant feedback on inputs,
                outputs and formulae.
                        </TextBlock>
                    </DockPanel>

                    <StackPanel
            HorizontalAlignment="Center"
            Margin="0,40,0,0"
            Orientation="Horizontal"
            Visibility="{Binding WaitingForTabVisibility}">
                        <!--<imaging:CrispImage Width="24" Height="24" Moniker="{x:Static catalog:KnownMonikers.StatusWarningOutline}" />-->

                        <Label>Waiting for a tab to open...</Label>
                    </StackPanel>

                    <StackPanel
            HorizontalAlignment="Center"
            Margin="0,40,0,0"
            Orientation="Vertical"
            Visibility="{Binding HasActiveProjectVisibility}">
                        <Button
                BorderThickness="0"
                Margin="10,0,0,0"
                Padding="10" Name="StartWatcherButton"
                Command="{Binding StartWatcherCommand}">
                            <!--<imaging:CrispImage Width="32" Height="32" Moniker="{x:Static catalog:KnownMonikers.Play}" />-->
                        </Button>

                        <TextBlock
                HorizontalAlignment="Center"
                Margin="10,5,0,0"
                Text="{Binding ActiveProjectPath}"
                TextWrapping="Wrap" />
                    </StackPanel>
                </StackPanel>

                <Grid Visibility="{Binding IsWatchingVisibility}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="50" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="50" />
                    </Grid.ColumnDefinitions>

                    <!--<imaging:CrispImage Width="24" Height="24" Moniker="{x:Static catalog:KnownMonikers.Watch}" />-->

                    <Label
            FontSize="14"
            Grid.Column="1"
            Grid.Row="0"
            Margin="10,5,0,0">
                        Watching
                    </Label>

                    <Button
                        Name="StopWatcherButton"
            Grid.Column="2"
            Grid.Row="0"
            Height="32"
            Margin="0,0,0,0"
            Width="32"
            Command="{Binding StopWatcherCommand}">
                        <!--<imaging:CrispImage Width="24" Height="24" Moniker="{x:Static catalog:KnownMonikers.Stop}" VerticalAlignment="Center" HorizontalAlignment="Center" />-->
                    </Button>

                    <TreeView
            BorderThickness="0"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            Grid.Row="1"
            ItemsSource="{Binding Watching}"
            Margin="-10,20,0,0">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate DataType="{x:Type xaml:TreeItem}">
                                <TreeViewItem IsExpanded="{Binding IsExpanded}" Margin="0,0,0,10">
                                    <TreeViewItem.Header>
                                        <DockPanel>
                                            <!--<imaging:CrispImage DockPanel.Dock="Left"
                                                    Height="16"
                                                    Width="16"
                                                    Moniker="{x:Static catalog:KnownMonikers.StatusOK}"
                                                    Visibility="{Binding IsSuccessVisibility}"/>

                                            <imaging:CrispImage DockPanel.Dock="Left"
                                                    Height="16"
                                                    Width="16"
                                                    Moniker="{x:Static catalog:KnownMonikers.StatusInvalid}"
                                                    Visibility="{Binding IsFailedVisibility}"/>-->
                                            <TextBlock
                                    DockPanel.Dock="Left"
                                    FontSize="14"
                                    Margin="10,4,0,5"
                                    Text="{Binding Name}" />
                                        </DockPanel>
                                    </TreeViewItem.Header>

                                    <StackPanel
                            Margin="15,0,0,0"
                            Orientation="Vertical"
                            Visibility="{Binding IsSuccessVisibility}">
                                        <TextBlock
                                FontWeight="Bold"
                                Margin="0,0,0,5"
                                Text="Inputs" />
                                        <ItemsControl ItemsSource="{Binding Result.CalculationDetail.Inputs}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Margin="10,0,0,5" Orientation="Horizontal">
                                                        <!--  <maths:FormulaControl  -->
                                                        <!--  Formula="{Binding Symbol}"  -->
                                                        <!--  Margin="0,4,10,0"  -->
                                                        <!--  Scale="15" />  -->
                                                        <TextBlock
                                                Text="{Binding DisplayName}"
                                                TextWrapping="Wrap"
                                                Width="125" />
                                                        <TextBlock Text="{Binding Value}" />
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <TextBlock
                                FontWeight="Bold"
                                Margin="0,20,0,5"
                                Text="Outputs" />
                                        <ItemsControl ItemsSource="{Binding Result.CalculationDetail.Outputs}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Margin="10,0,0,5" Orientation="Horizontal">
                                                        <TextBlock
                                                Text="{Binding DisplayName}"
                                                TextWrapping="Wrap"
                                                Width="125" />
                                                        <TextBlock Text="{Binding Value}" />
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <TextBlock
                                FontWeight="Bold"
                                Margin="0,20,0,5"
                                Text="Formulae" />

                                        <ItemsControl ItemsSource="{Binding Result.CalculationDetail.Formulae}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Margin="10,0,0,25" Orientation="Vertical">
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock
                                                    FontWeight="Bold"
                                                    Text="Ref."
                                                    Width="30" />
                                                            <TextBlock Text="{Binding Ref}" TextWrapping="Wrap" />
                                                        </StackPanel>

                                                        <TextBlock
                                                FontWeight="Bold"
                                                Margin="0,10,0,5"
                                                Text="Narrative" />
                                                        <TextBlock Text="{Binding Narrative}" TextWrapping="Wrap" />

                                                        <!--  <ItemsControl  -->
                                                        <!--  ItemsSource="{Binding Expressions}"  -->
                                                        <!--  Margin="0,20,0,0"  -->
                                                        <!--  Visibility="{Binding ExpressionVisibility}">  -->
                                                        <!--     <ItemsControl.ItemTemplate> -->
                                                        <!--         <DataTemplate> -->
                                                        <!--             <maths:FormulaControl Formula="{Binding}" Scale="20" /> -->
                                                        <!--         </DataTemplate> -->
                                                        <!--     </ItemsControl.ItemTemplate> -->
                                                        <!-- </ItemsControl> -->
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <StackPanel Orientation="Vertical" Visibility="{Binding IsFailedVisibility}">
                                        <TextBlock Text="{Binding Result.Failure.Message}" />
                                    </StackPanel>
                                </TreeViewItem>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </Grid>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
